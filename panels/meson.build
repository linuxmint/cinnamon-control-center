
if colord.found()
  subdir('color')
endif

subdir('common')

subdir('display')

if libnm.found()
  subdir('network')
endif

if goa.found()
  subdir('online-accounts')
endif

subdir('region')

if libwacom.found()
  subdir('wacom')
endif
